# Server-side dependencies (Flask app + shared lib) — deployed to Heroku.
# Python is pinned to 3.12 via .python-version.

# Web app
flask>=3.0
flask-login>=0.6
gunicorn>=21.0

# AWS / storage
boto3>=1.34

# Messaging
paho-mqtt         # MQTT — every platform (Flask, Pi, browser-side WS URL)
twilio>=8.0       # inbound SMS webhook signature validation

# HTTP (MessageManager seed)
requests>=2.31

# Provisioner (scripts/provision-pi.sh password fallback). Only the
# laptop side uses this — the askpass binary decrypts the encrypted
# password file with Fernet (AES + HMAC). Server-side (Heroku) also
# benefits since cryptography is small and pure-Python on most platforms.
cryptography>=42.0
