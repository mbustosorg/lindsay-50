## 1. Shared status store

- [ ] 1.1 Create `lib_shared/sign_status.py` with the `LatestSignStatus` class: `threading.RLock`-guarded in-memory store with `update(snapshot_dict)` (validates required keys), `snapshot() -> dict | None` (defensive copy), and `received_at_wallclock() -> str | None` (ISO-8601 wall-clock timestamp at which the most recent update landed). The store is a thin holder for the latest snapshot — it does NOT compute state (state is browser-side per the design). Constants for the required-keys list (`REQUIRED_SNAPSHOT_KEYS = (...)`) are exported so the test suite and any future config layer can reference them by name.
- [ ] 1.2 Add `tests/test_sign_status.py` with round-trip tests: `update()` stores the dict under the lock, `snapshot()` returns a defensive copy (mutating the returned dict does not affect the store), `received_at_wallclock()` returns an ISO-8601 string after `update()` and `None` before any `update()`, `snapshot()` on an empty store returns `None`, and a payload missing a required key is rejected (raises ValueError, store unchanged). Cover the concurrent-update case (two threads call `update()` simultaneously — the lock prevents torn reads).

## 2. Device-side status publish

- [ ] 2.1 In `heart-matrix-controller/status.py`, **reshape the `StatusSnapshot` dataclass** — drop the three dead fields and add `short_sha`:
  - **Drop** `pid` (no consumer reads it; the loader's `read_status` checks its presence but `_is_status_healthy` never reads the value).
  - **Drop** `messages_rendered` (no consumer reads it; the deque-length peek in `main.py` is a 5-line excursion into `InMemoryMessages` private internals for a value no one acts on).
  - **Drop** `last_tick_age_ms` (the `_LAST_TICK_MONOTONIC` global in `main.py` is initialized to `0.0` and never reassigned, so the field always reads 0 — a dead signal masquerading as a health check).
  - **Add** `short_sha: str = ""` (the first 7 chars of `active_sha`; imported from `lib_shared.boot_config.short_sha` at write time so it's derived in one place).
  - **Change** `uptime_seconds: float = 0.0` → `uptime_seconds: int = 0` (truncate to whole seconds — fractional seconds are noise in a heartbeat published every 5s).
  - The final field set: `schema_version`, `active_sha`, `short_sha`, `started_at`, `updated_at`, `uptime_seconds`, `mqtt_connected`, `last_error` (8 fields).
  - **`to_dict()` is the single serializer** for both the `.status.json` file write and the MQTT wire payload (no more `to_mqtt_dict()` asymmetry).
  - In `read_status()`, drop `pid` from the required-keys tuple (the presence check was the only thing that ever read it).
  - **Also change the existing `StatusWriter` throttle from `STATUS_WRITE_INTERVAL_S = 3.0` to `STATUS_WRITE_INTERVAL_S = 5.0`** (still well under the loader's updated 17s probe hold; the 5s value is now shared with the MQTT publish path). The `tick()` method is extended to publish the MQTT envelope in the same call as the file write.
- [ ] 2.1a In `heart-matrix-controller/loader.py`, change `BOOT_HOLD_S` from `8.0` to `17.0` to match the new 5s `.status.json` write cadence. Update the constant's docstring/comment to document the new derivation: "3× status.json writes (5s each) + 2s slack — aligned with the dashboard pill's 15s `live` window so the loader and the UI read the same '3 missed writes' signal at the same scale." The loader continues to use `.status.json` mtime as its sole health-check signal; this change does NOT add MQTT-based loader logic.
- [ ] 2.2 Create `heart-matrix-controller/status_publisher.py` with the `StatusPublisher` class: long-lived paho publisher for the status flow. Constructor: opens a single `mqtt.Client`, calls `connect_async(...)` + `loop_start()`. `publish(payload_dict) -> bool`: calls `client.publish(topic, payload.encode(), qos=0)` (thread-safe, non-blocking — the loop thread handles the network); returns True on success, False on any error or non-success `rc`. On a `publish()` returning a non-success `rc`, a `threading.Timer` schedules a reconnect attempt 5 seconds later. `close()`: calls `loop_stop()` + `disconnect()`. The existing `publish_envelope` (in `lib_shared/paho_mqtt_client.py`) keeps its fresh-client-per-call pattern — `StatusPublisher` is a separate, status-only path.
- [ ] 2.2a In `heart-matrix-controller/main.py`, update the snapshot builder call: drop the `_LAST_TICK_MONOTONIC` global, the `last_tick_monotonic` parameter, the `_msgs` deque read, and the `pid=os.getpid()` / `last_tick_age_ms=last_tick_ms` / `messages_rendered=messages_rendered` keyword args. Add `short_sha=short_sha(_ACTIVE_SHA)` (imported from `lib_shared.boot_config`). Change `uptime_seconds=now_monotonic - _STARTED_AT_MONOTONIC` to `uptime_seconds=int(now_monotonic - _STARTED_AT_MONOTONIC)` (truncate to whole seconds). The lambda passed to `make_status_writer` becomes `lambda: _build_status_snapshot()` (no more closure over a dead variable).
- [ ] 2.2b In `heart-matrix-controller/loader.py`, drop the `last_tick_age_ms` check from `_is_status_healthy` — the field is gone from the snapshot, so the loader can no longer key off it. The new healthy contract is `mqtt_connected === true` AND `last_error is None` (two checks, not three). Update the `probe()` docstring's healthy-criteria bullet list to match. Update the `max_tick_age_ms` parameter (and its default) to a no-op for backward compatibility OR drop the parameter entirely; pick whichever is less churn in the existing tests.
- [ ] 2.3 In `heart-matrix-controller/main.py`, instantiate `StatusPublisher` at startup (after the existing MQTT client setup) and pass it to `StatusWriter` (or call `StatusPublisher.publish()` directly from `StatusWriter.tick()`). On shutdown, call `StatusPublisher.close()` in the existing `try/finally` around the render loop. The `StatusWriter.tick()` method now does: build snapshot → call `.to_dict()` (single serializer for both wire and file) → call `os.replace(.status.json, ...)` (existing) → call `_status_publisher.publish(payload)` (new) — all in the same `tick()` call, at the 5s throttle.
- [ ] 2.4 Add `MQTT_STATUS_TOPIC` to `REQUIRED_KEYS` in `heart-matrix-controller/main.py`. The config reader's env-override path already takes precedence over `settings.toml`, so no new logic is needed — just declare the key. The resolution helper `MQTT_STATUS_TOPIC or f"{MQTT_TOPIC}-status"` lives in `main.py` and feeds both the `StatusPublisher` constructor and the `REQUIRED_KEYS` check.
- [ ] 2.5 In `lib_shared/paho_mqtt_client.py`, extend `PahoMqttClient.__init__` with optional `status_dispatch_callback` + `status_topic` parameters (Flask-side subscriber extension; the Pi-side publisher is a separate class, see 2.2). The existing `dispatch_callback` + `topic` are unchanged. In the subscriber loop, `on_connect` calls `client.subscribe([(topic, 1), (status_topic, 1)])` when both are set; `on_message` dispatches by `msg.topic` to the right callback. The two callbacks' exception paths MUST be isolated — a raise in `status_dispatch_callback` does not affect `dispatch_callback` and vice versa. The `publish_envelope` method is unchanged.

## 3. Flask-side status subscribe + endpoint

- [ ] 3.1 In `heart-message-manager/main.py`, instantiate `latest_status = LatestSignStatus()` at module level. Build a `status_dispatch_callback` that JSON-parses the payload, validates required keys (using the `REQUIRED_SNAPSHOT_KEYS` constant), and calls `latest_status.update(parsed)`. Pass both `dispatch_callback` and `status_dispatch_callback` to `PahoMqttClient` with the resolved `MQTT_TOPIC` and `MQTT_STATUS_TOPIC` topics. Add `MQTT_STATUS_TOPIC` to the Flask-side required-config-keys list.
- [ ] 3.2 Add `GET /api/sign-status` endpoint in `heart-message-manager/main.py`. **Always returns HTTP 200** with the shape `{snapshot: latest_status.snapshot(), received_at: latest_status.received_at_wallclock()}`. The endpoint MUST NOT compute or include a `state` field — state computation is browser-side per the design. When the store is empty, both `snapshot` and `received_at` are `null`.
- [ ] 3.3 Add the `MQTT_STATUS_TOPIC` resolution helper (`MQTT_STATUS_TOPIC or f"{MQTT_TOPIC}-status"`) in `heart-message-manager/main.py` alongside the existing `_cfg_required_keys` block. Wire the resolved value into the Flask `window.APP_CONFIG` JS injection (add `mqttStatusTopic`) so `sign_status.js` can read it from `window.APP_CONFIG.mqttStatusTopic`.

## 4. Browser-side load-time fetch + WS subscription

- [ ] 4.1 In `heart-message-manager/templates/dashboard.html`, replace the hardcoded green "Live" pill with `<span id="sign-live-pill" data-state="offline" class="px-4 py-2 rounded-full bg-slate-100 text-slate-600 text-sm font-semibold flex items-center gap-2"><span class="w-2 h-2 bg-slate-400 rounded-full"></span>Offline</span>`. The new placeholder uses `data-state="offline"` as the initial state and "Offline" as the initial text. `sign_status.js` will toggle both based on the browser-computed state.
- [ ] 4.2 Create `heart-message-manager/static/sign_status.js` (new ES module). On DOM ready, the module: (a) fires **one** `fetch('/api/sign-status')` and stores the response in the in-memory snapshot IF `response.snapshot.updated_at` is newer than any WS-received snapshot (otherwise drop the fetch response); (b) opens a second `createMqttWsClient` instance with `{mqttWsUrl, username, password, topic: mqttStatusTopic, onEnvelope: (payload) => decodeAndStoreSnapshot(payload)}` — the `onEnvelope` callback decodes the JSON payload, validates required keys, and replaces the in-memory snapshot; (c) starts a 5-second `setInterval` that re-evaluates state AND health and re-renders the DOM; (d) wires `setInterval` cleanup to `beforeunload`. The module exports the `LIVE_THRESHOLD_S = 15`, `UNKNOWN_THRESHOLD_S = 30`, `stateFromAge(ageSeconds) -> "live" | "unknown" | "offline"`, and `healthFromSnapshot(snapshot) -> "healthy" | "degraded"` helpers as named exports. State × health → pill rendering: `live + healthy` → green classes + pulse class + text "Live"; `live + degraded` → amber classes + no-pulse + text "Degraded"; `unknown` → amber classes + no-pulse + text "Unknown"; `offline` → grey classes + no-pulse + text "Offline". The WS client's connection state is surfaced to a small secondary indicator next to the pill (e.g., `<span id="sign-status-ws-state">…</span>`), parallel to the existing `#mqtt-status` pattern.
- [ ] 4.3 Include `<script src="{{ url_for('static', filename='sign_status.js') }}" defer></script>` in `heart-message-manager/templates/base.html` so the script loads on every authenticated page (not just dashboard — keeps the logic DRY and matches the existing `mqtt_ws_client.js` inclusion pattern). The script MUST short-circuit gracefully (no-op) on pages that have neither `#sign-live-pill` nor `[data-sign-status-field]` (so it's safe to include globally).

## 5. Settings page Sign Health section

- [ ] 5.1 In `heart-message-manager/templates/settings.html`, add a new **Sign Health** section at the top (above "Sign Name") with read-only spans for `active_sha`, `short_sha`, `started_at`, `uptime_seconds` (formatted via a JS helper), `mqtt_connected`, `last_error`, and `received_at_browser`. Use `data-sign-status-field="<name>"` attributes so `sign_status.js` can populate the slots in place. The snapshot-field slots MUST be wrapped in a container with `data-sign-status-fields` that the JS hides when state is `"offline"`. A separate placeholder span with `data-sign-status-placeholder` shows the text "No status received yet" and is hidden when state is NOT `"offline"`. When `health="degraded"`, a warning banner with `data-sign-status-degraded-banner` surfaces at the top of the field table naming the failing health check (e.g., "MQTT disconnected", "Last error: <message>").
- [ ] 5.2 Extend `sign_status.js` (the same module from 4.2) to populate the Settings-page slots via the existing `[data-sign-status-field="<name>"]` selector. The in-memory snapshot is shared between the dashboard pill (4.1) and the settings slots (5.1) — one WS subscription, one load-time fetch, two consumers. The module renders field values when state is `"live"` or `"unknown"`, hides field slots and shows the placeholder when state is `"offline"`. The `received_at_browser` field shows the timestamp the browser received the snapshot (the browser's `Date.now()` at WS-message-arrival time, or at fetch-response-arrival time for the load-time fetch). The degraded warning banner is shown when `health="degraded"` and the snapshot is in memory; it names the failing health check (e.g., "MQTT disconnected" if `mqtt_connected === false`, "Last error: <message>" if `last_error` is set).
- [ ] 5.3 Format `uptime_seconds` as `Xd Yh Zm` in the JS (e.g., `90061` → `1d 1h 1m`). Use a single helper function; the snapshot value is an integer number of seconds.

## 6. Settings.toml + env config

- [ ] 6.1 In `heart-matrix-controller/settings.toml.example`, add `MQTT_STATUS_TOPIC = ""` (empty = derive from `MQTT_TOPIC`) and the standard env-override note (`MQTT_STATUS_TOPIC` env var takes precedence).
- [ ] 6.2 In `heart-message-manager/settings.toml.example`, add the same line + env-override note.
- [ ] 6.3 Add the standard `cp settings.toml.example settings.toml` step to the README quick-start (if there is one for this repo) — the new key is required for the feature but the operator may not know to add it.

## 7. Tests

- [ ] 7.1 `tests/test_sign_status.py` (added in 1.2) — covers lock semantics, defensive-copy semantics, `received_at_wallclock()` ISO-8601 round-trip, empty-store behavior, missing-required-keys rejection, and concurrent-update.
- [ ] 7.2 `tests/test_status_publisher.py` — covers `StatusPublisher` lifecycle: constructor calls `connect_async` + `loop_start`; `publish()` calls `client.publish` with `qos=0`; `publish()` is thread-safe (concurrent calls from multiple threads do not raise); `close()` calls `loop_stop` + `disconnect`; on `publish()` returning a non-success rc, a reconnect timer is scheduled. Uses a mock `mqtt.Client` (assert that `connect_async` and `loop_start` are called once at construction, that `publish` is called with `qos=0` and the encoded payload, and that `close()` pairs `loop_stop` with `loop_start`).
- [ ] 7.3 `tests/test_sign_status_endpoint.py` — covers the new Flask endpoint: always returns 200; returns `{snapshot: {...}, received_at: <iso>}` when the store has a snapshot; returns `{snapshot: null, received_at: null}` when the store is empty. The endpoint MUST NOT compute or return a `state` field — the test asserts the response body has no `state` key. Use the Flask test client + a synthetic `LatestSignStatus` injected via monkeypatch.
- [ ] 7.4 `tests/test_status_to_dict.py` — covers the `StatusSnapshot.to_dict` serializer: produces a JSON-roundtrip-safe dict with the new field set (`schema_version`, `active_sha`, `short_sha`, `started_at`, `updated_at`, `uptime_seconds`, `mqtt_connected`, `last_error`); explicitly asserts the dict has NO `pid`, NO `messages_rendered`, and NO `last_tick_age_ms` keys (round-trip safety against re-introducing the dropped fields); asserts `uptime_seconds` is an `int` (truncated, not float). Also covers the `read_status` required-keys change: a payload missing `pid` is accepted (not rejected); a payload missing `active_sha` is still rejected.
- [ ] 7.5 `tests/test_paho_status_subscribe_dispatch.py` — covers `PahoMqttClient` extension: when both `dispatch_callback` and `status_dispatch_callback` are set, `on_message` dispatches by `msg.topic` to the right callback; a raise in one callback does not affect the other. Uses a fake `msg` object with a `.topic` attribute.
- [ ] 7.6 `tests/test_status_writer_integration.py` — covers the unified `StatusWriter.tick()`: when the throttle is past, `tick()` writes the file (existing) AND publishes the MQTT envelope (new) in the same call; when the throttle is not past, `tick()` is a no-op. Uses a mock `StatusPublisher`.
- [ ] 7.7 `tests/test_health_from_snapshot.py` — covers `healthFromSnapshot(snapshot)`: returns `"healthy"` when `mqtt_connected === true` and `last_error` is null/empty; returns `"degraded"` when `mqtt_connected === false`; returns `"degraded"` when `last_error` is a non-empty string; treats empty `last_error` string as null (healthy). The `HEALTH_TICK_AGE_MAX_MS` constant no longer exists — the function keys off two signals, not three.
- [ ] 7.8 `tests/test_loader_boot_hold.py` — covers the loader's `BOOT_HOLD_S` constant: asserts the value is exactly `17.0` after the change; asserts the value is documented in a comment as "3× status.json writes (5s each) + 2s slack". The test reads the source file directly (or imports the constant) and checks the value and the comment.
- [ ] 7.9 Update existing tests to drop `pid` / `messages_rendered` / `last_tick_age_ms` references and add `short_sha` / int-`uptime_seconds` assertions: `tests/test_status.py` — drop the `assert snap.pid == 0`, `assert snap.last_tick_age_ms == 0`, and `assert snap.messages_rendered == 0` lines; drop the `last_tick_age_ms=42` and `messages_rendered=10` test fixtures; change `assert snap.uptime_seconds == 0.0` to `assert snap.uptime_seconds == 0` and the `uptime_seconds=60.0` fixture to `uptime_seconds=60`. Add a `short_sha` test fixture and assert the field defaults to `""`. `tests/test_loader.py` — drop `"last_tick_age_ms": 100,` and `"messages_rendered": 0,` from the snapshot fixture; drop the `_is_status_healthy({"mqtt_connected": True, "last_tick_age_ms": 9999, "last_error": None}) is False` test (the `last_tick_age_ms` signal no longer gates the loader); keep the `mqtt_connected=False` and `last_error="boom"` tests (those are the two remaining health signals). `heart-matrix-controller/main.py` snapshot builder — drop the `_msgs` deque read, the `last_tick_monotonic` parameter, the `_LAST_TICK_MONOTONIC` global, and the `pid=os.getpid()` / `last_tick_age_ms=last_tick_ms` / `messages_rendered=messages_rendered` keyword args; add `short_sha=short_sha(_ACTIVE_SHA)` and cast `uptime_seconds` to int. These are existing-code touch-ups, not new tests.
- [ ] 7.10 `tests/test_short_sha_on_snapshot.py` — covers the `short_sha` derivation on the snapshot: a `StatusSnapshot` with `active_sha="abc1234567890def1234567890def1234567890"` has `short_sha="abc1234"`; an empty `active_sha` gives an empty `short_sha` (consistent empty state); a full-7-char SHA like `active_sha="abc1234"` gives `short_sha="abc1234"` (idempotent on short inputs).

## 8. Documentation + manual verification

- [ ] 8.1 Update `CLAUDE.md` "Architecture" section to include the new status flow in the diagram (Pi → `StatusWriter.tick()` → `.status.json` (loader) + `StatusPublisher.publish()` → MQTT_STATUS_TOPIC → Flask subscriber → in-memory store → `/api/sign-status` (one-shot load-time hydration) → Browser WS subscription for live updates → Dashboard pill + Settings page).
- [ ] 8.2 Document the Adafruit IO feed creation step in `README.md` (or the relevant deployment doc): the operator must create the new `lindsay-50-status` feed in the AIO dashboard before the first publish lands, otherwise the broker will silently reject the publish.
- [ ] 8.3 Document the `.status.json` cadence change (3s → 5s) in the `loader.py` docstring or its config comments so the loader's 8s probe headroom stays visible.
- [ ] 8.4 Run `PYTHONPATH=. pytest tests/ -v` (per the project config) and confirm all new + existing tests pass before opening the PR.
- [ ] 8.5 Manual smoke test on the Pi: SSH to the Pi, `systemctl restart lindsay_50`, watch `journalctl -u lindsay_50 -f` for "publish_status" log lines every 5s, then load the admin dashboard in a browser and confirm the pill flips from grey ("Offline") to green ("Live") within ~5s of the first publish. Stop watching the dashboard for 30+ seconds (longer than `UNKNOWN_THRESHOLD_S = 30`) and confirm the pill flips from green → amber → grey on the local 5-second interval. Test the health-aware pill by killing the broker connection (e.g., `sudo iptables -A OUTPUT -p tcp --dport 8883 -j DROP` or `sudo systemctl stop mosquitto` on a local broker) and confirm the pill flips from green "Live" to amber "Degraded" within ~5s (the next snapshot will report `mqtt_connected: false`).